# AI Quality Gate — Backend

FastAPI service that evaluates LLM/RAG applications before release and returns
PASS / WARN / BLOCK release decisions.

See the repository root `PROJECT_STATE.md` for architecture and run instructions.
